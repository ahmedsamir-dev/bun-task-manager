# lefthook.yml
pre-commit:
  commands:
    prettier:
      run: bunx prettier --write ./src && git add {staged_files}
    # test:
    #   run: bun run test
pre-push:
  commands:
    # test:
    #   run: bun run test